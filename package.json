{
  "name": "jev-gate",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: one Jev decision per user turn, a lossless execution brief, and native Opus/Fable delegation hints for a Sonnet main session.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "pack": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  }
}
